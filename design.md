# 이번 편 디자인 지정

current_style: LIFEPLUS

> 기본값: LIFEPLUS (한화생명 공식 브랜드 스타일)
> 스타일 변경하려면 `LIFEPLUS` → `wired` / `stripe` / `toss` 등으로 교체.
> 사용 가능한 스타일은 `designs/` 폴더 참조.
> 새 스타일 추가: `designs/<새이름>.md` 파일 생성.
